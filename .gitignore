# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
# `.env*.local` alone does NOT match a bare `.env`, so a file created with that exact name
# would have been committed with live credentials in it. Ignore every .env variant and
# un-ignore only the checked-in template.
.env
.env.*
!.env.example

# Common ignores
node_modules
.next/
*.tsbuildinfo
.DS_Store
